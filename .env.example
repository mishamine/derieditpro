# Copy to `.env` locally if you want OG URLs during dev builds (optional).
# Production CI sets this via GitHub Actions — see `.github/workflows/deploy.yml`.
#
# Public site (GitHub Pages project): https://juneaucross.github.io/super-deri/
# Repo: https://github.com/juneaucross/super-deri
#
# Preview image: `public/og.png` (recommended 1200×630).
VITE_SITE_URL=https://deriedit.pro
